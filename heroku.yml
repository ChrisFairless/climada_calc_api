setup:
  addons:
    - plan: heroku-redis:hobby-dev
build:
  docker:
    web:
      dockerfile: ./Dockerfile
    geocode_api:
      dockerfile: ./Dockerfile.heroku.osmnames

run:
  web: bash -c "python manage.py runserver 0.0.0.0:\$PORT"
  worker: 
    command: 
      - celery -A calc_api worker -l info
    image: web
